#pragma once
#include "alignment-metrics-options.h"

namespace xoos::alignment_metrics {
// Run alignment_metrics with the given options
void RunAlignmentMetrics(const AlignmentMetricsOptions& opts);
}  // namespace xoos::alignment_metrics
